# Our blueprints

## Why do we use blueprints

## Implementation

!!! warning
    When we define the layout of our blueprints, we still need to access the `DashBlueprint` instance itself somehow 
    because translatable texts (that are part of the layout) are wrapped within blueprints as well. Like any other
    blueprints, these translatable texts must be embedded into an `dash.app` or a `DashBlueprint` instance.

## Other section
